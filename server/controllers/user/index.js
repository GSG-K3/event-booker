const login = require('./login');
const profile = require('./profile');
const signup = require('./signup');
const userEvent = require('./userEvent');
const logout = require('./logout');

module.exports = { login, profile, signup, userEvent, logout };
